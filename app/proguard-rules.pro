# The app uses Android framework components declared in AndroidManifest.xml and no
# reflection-based model serialization. Keep this file intentionally minimal so R8
# can remove dead code, inline helpers and rename implementation classes freely.
