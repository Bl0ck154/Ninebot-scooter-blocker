name: Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Decode stable signing keystore
        run: |
          mkdir -p ci
          base64 -d ci/stable-debug-keystore.b64 > ci/ninebot-debug.keystore

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android 35
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Test exact SHU Ninebot protocol
        run: gradle --no-daemon :app:testDebugUnitTest

      - name: Build stable-signed APK
        run: gradle --no-daemon :app:assembleDebug

      - name: Verify APK signature
        run: |
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose --print-certs app/build/outputs/apk/debug/app-debug.apk

      - name: Rename APK
        run: cp app/build/outputs/apk/debug/app-debug.apk Ninebot-scooter-blocker-v0.5.1.apk

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: Ninebot-scooter-blocker-v0.5.1-apk
          path: Ninebot-scooter-blocker-v0.5.1.apk
          if-no-files-found: error

      - name: Publish v0.5.1 release APK
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view v0.5.1 >/dev/null 2>&1; then
            gh release upload v0.5.1 Ninebot-scooter-blocker-v0.5.1.apk --clobber
          else
            gh release create v0.5.1 Ninebot-scooter-blocker-v0.5.1.apk \
              --target "${GITHUB_SHA}" \
              --title "Ninebot Scooter Blocker v0.5.1" \
              --notes "Diagnostic build: fixes Android BLE discovery by reading ScanRecord names and no longer dropping unnamed devices; uses low-latency unfiltered scan; adds scrollable/copyable logs with scan advertising bytes, GATT services, TX/RX and exact SHU lock frame. Protocol command itself is unchanged from v0.5.0."
          fi
